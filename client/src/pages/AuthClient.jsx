import React from 'react';
import Auth from "../components/admin/auth/Auth";

const AuthClient = () => {
    return (
        <div>
            <Auth/>
        </div>
    );
};

export default AuthClient;