import React from 'react';
import Form from "../components/form/Form";

const CreateDevice = () => {
    return (
        <div>
            <Form/>
        </div>
    );
};

export default CreateDevice;